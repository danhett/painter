/**
 * Config.
 *
 * @author Dan Hett / RITUALS (hellodanhett@gmail.com)
 */
class Config
{
  // Shows the console on the main projection screen for debugging in real life
  static final boolean CONSOLE = false;

  // Sets the app to performance mode, hiding tools etc
  static final boolean PERFORMANCE = true;

  // Does what it says on the tin, just for the projection surface
  static final boolean FULLSCREEN = false;

  // For stuff that isn't full screen, controls the surface sizes
  static final int WIDTH = 800;
  static final int HEIGHT = 450;

  Config()
  {

  }
}
